error:
  type: AgentExecutionError
  message: Maximum retry attempts reached for model call.
  details:
    agent_component: llm
    middleware: ModelRetryMiddleware
    attempts: 3
    error: 'Simulated 401: Invalid API key provided (Attempt 3)'
    llm_model: FakeChatModel()
