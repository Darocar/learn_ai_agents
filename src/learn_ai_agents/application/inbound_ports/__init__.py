"""
Inbound Ports

Inbound ports are interfaces that define how the outside world can interact with
the application (e.g., REST API endpoints, CLI commands, message consumers).

These are typically implemented by adapters in the infrastructure layer.
"""
